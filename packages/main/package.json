{
  "name": "@pomello-desktop/main",
  "version": "1.0.0",
  "private": true,
  "description": "Main process for Pomello desktop app",
  "license": "UNLICENSED",
  "author": "Tiny Nudge LLC <pomello@tinynudge.co>",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "clean": "rimraf dist",
    "format": "prettier --write . --ignore-path ../../.prettierignore",
    "format:check": "prettier --check . --ignore-path ../../.prettierignore",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pomello-desktop/domain": "workspace:*",
    "ajv": "^8.17.1",
    "electron-store": "^8.2.0",
    "electron-updater": "^6.6.2",
    "throttle-debounce": "^5.0.2",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/throttle-debounce": "^5.0.2"
  }
}
